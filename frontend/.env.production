# Production build config, read by Vite automatically for `npm run build`.
# Root origin only - src/api/client.ts appends /api/v1 itself. See
# Documents/DEPLOYMENT.md step 4: one origin for both frontend and API via
# CloudFront's /api/* behavior, so no CORS complications.
VITE_API_BASE_URL=https://d24gbkw5emat7s.cloudfront.net
